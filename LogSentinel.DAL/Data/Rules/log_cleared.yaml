name: Security Log Cleared
description: Detects when security event log is cleared
severity: Critical
enabled: true
selection:
  event_id: 1102
condition:
  always: true
action:
  alert: true
  title: "Security Log Cleared"
  description: "Security event log was cleared on {host} by {user}"
