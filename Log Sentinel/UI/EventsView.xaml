<UserControl x:Class="Log_Sentinel.UI.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Log_Sentinel.UI">
    <Border Background="#F8FAFC" Margin="20">
        <StackPanel>
            <TextBlock Text="Events" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- Ô tìm kiếm -->
            <Grid Width="250" Margin="0,0,0,15">
                <TextBox x:Name="SearchBox" VerticalContentAlignment="Center" Margin="0"/>
                <TextBlock Text="Search logs..."
                           Foreground="Gray"
                           Margin="8,0,0,0"
                           IsHitTestVisible="False"
                           VerticalAlignment="Center"
                           Visibility="{Binding Text, ElementName=SearchBox, Converter={StaticResource EmptyToVisibilityConverter}}"/>
            </Grid>

            <!-- Danh sách sự kiện -->
            <DataGrid AutoGenerateColumns="False" HeadersVisibility="Column"
                      CanUserAddRows="False" BorderThickness="0"
                      RowBackground="#FFFFFF" AlternatingRowBackground="#F1F5F9">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="150"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="100"/>
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Border>
</UserControl>
