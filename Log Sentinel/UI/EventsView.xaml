<UserControl x:Class="Log_Sentinel.UI.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#F8FAFC" Margin="20">
        <StackPanel>
            <TextBlock Text="Events" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

            <!-- Search and Filter Controls -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                <!-- Search Box with placeholder -->
                <Border Background="White" CornerRadius="5" Padding="10" Margin="0,0,15,0" Width="300">
                    <Grid>
                        <TextBox x:Name="SearchBox" 
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalContentAlignment="Center"
                                 FontSize="14" />
                        <TextBlock Text="Search logs..." 
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Left"
                                   Margin="5,0,0,0"
                                   Foreground="LightGray"
                                   IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <!-- Source Filter -->
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Source:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="Medium"/>
                    <ComboBox x:Name="SourceFilterComboBox"
                              SelectedValue="{Binding SourceFilter}"
                              Width="150"
                              Height="30">
                        <ComboBoxItem Content="All" />
                        <ComboBoxItem Content="Sample" />
                        <ComboBoxItem Content="WindowsEventLog" />
                        <ComboBoxItem Content="Sysmon" />
                        <ComboBoxItem Content="System" />
                    </ComboBox>
                </StackPanel>
            </StackPanel>

            <!-- Events List -->
            <DataGrid x:Name="EventsDataGrid"
                      AutoGenerateColumns="False" 
                      HeadersVisibility="Column"
                      CanUserAddRows="False" 
                      BorderThickness="1"
                      BorderBrush="#E2E8F0"
                      RowBackground="#FFFFFF" 
                      AlternatingRowBackground="#F9FAFB"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E5E7EB"
                      Height="400"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="120"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="100"/>
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Border>
</UserControl>
