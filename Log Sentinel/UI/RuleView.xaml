<UserControl x:Class="Log_Sentinel.UI.RuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#F8FAFC" Margin="20">
        <StackPanel>
            <TextBlock Text="Rules" FontSize="22" FontWeight="Bold" Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox Width="250" Height="30" VerticalContentAlignment="Center" Margin="0"/>
                <Button Content="Add Rule" 
                        Background="#2563EB" 
                        Foreground="White" 
                        BorderThickness="0" 
                        Padding="10,5"
                        Margin="5,0,0,0"
                        Cursor="Hand"/>
            </StackPanel>

            <ListView>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8" Margin="0,5">
                            <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                                <TextBlock Text="{Binding Name}" Width="200" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Condition}" Width="300" VerticalAlignment="Center"/>
                                <Button Content="Edit" Width="60" Margin="5,0"/>
                                <Button Content="Delete" Width="60" Margin="5,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Border>
</UserControl>
