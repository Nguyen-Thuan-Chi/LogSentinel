<UserControl x:Class="Log_Sentinel.UI.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Log_Sentinel.UI">
    <UserControl.Resources>
        <local:SeverityToColorConverter x:Key="SeverityToColorConverter"/>
    </UserControl.Resources>
    
    <Border Background="#F8FAFC" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="Alert Management" FontSize="26" FontWeight="Bold" HorizontalAlignment="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="? Acknowledge All" 
                            Command="{Binding AcknowledgeAllCommand}"
                            Background="#10B981" 
                            Foreground="White" 
                            BorderThickness="0" 
                            Padding="12,8"
                            Margin="0,0,10,0"
                            Cursor="Hand"/>
                    <Button Content="?? Export CSV" 
                            Command="{Binding ExportCsvCommand}"
                            Background="#3B82F6" 
                            Foreground="White" 
                            BorderThickness="0" 
                            Padding="12,8"
                            Margin="0,0,10,0"
                            Cursor="Hand"/>
                    <Button Content="?? Refresh" 
                            Command="{Binding RefreshCommand}"
                            Background="#6B7280" 
                            Foreground="White" 
                            BorderThickness="0" 
                            Padding="12,8"
                            Cursor="Hand"/>
                </StackPanel>
            </Grid>

            <!-- Filter Options -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                <TextBlock Text="Severity:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <ComboBox SelectedItem="{Binding SelectedSeverity, Mode=TwoWay}"
                          Width="150"
                          Height="35"
                          Padding="10"
                          BorderBrush="#CBD5E1"
                          BorderThickness="1">
                    <ComboBoxItem Content="All" IsSelected="True"/>
                    <ComboBoxItem Content="Critical"/>
                    <ComboBoxItem Content="High"/>
                    <ComboBoxItem Content="Medium"/>
                    <ComboBoxItem Content="Low"/>
                </ComboBox>

                <CheckBox Content="Show Acknowledged" 
                          IsChecked="{Binding ShowAcknowledged}"
                          VerticalAlignment="Center"
                          Margin="20,0,0,0"/>
            </StackPanel>

            <!-- Alerts DataGrid -->
            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding FilteredAlerts}"
                      SelectedItem="{Binding SelectedAlert}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      BorderBrush="#CBD5E1"
                      BorderThickness="1"
                      AlternatingRowBackground="#F1F5F9"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E2E8F0">
                <DataGrid.Columns>
                    <!-- Severity -->
                    <DataGridTemplateColumn Header="Severity" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{Binding Severity, Converter={StaticResource SeverityToColorConverter}}"
                                       CornerRadius="4"
                                       Padding="6,2"
                                       HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Severity}" 
                                              Foreground="White"
                                              FontWeight="SemiBold"
                                              FontSize="11"
                                              HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Timestamp -->
                    <DataGridTextColumn Header="Time" 
                                       Binding="{Binding Timestamp, StringFormat='yyyy-MM-dd HH:mm:ss'}" 
                                       Width="140"/>

                    <!-- Rule Name -->
                    <DataGridTextColumn Header="Rule" 
                                       Binding="{Binding RuleName}" 
                                       Width="200"
                                       FontWeight="SemiBold"/>

                    <!-- Title -->
                    <DataGridTextColumn Header="Title" 
                                       Binding="{Binding Title}" 
                                       Width="2*"/>

                    <!-- Description -->
                    <DataGridTextColumn Header="Description" 
                                       Binding="{Binding Description}" 
                                       Width="3*"/>

                    <!-- Status -->
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StatusText}" 
                                          FontWeight="SemiBold"
                                          HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAcknowledged}" Value="True">
                                                    <Setter Property="Foreground" Value="#10B981"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsAcknowledged}" Value="False">
                                                    <Setter Property="Foreground" Value="#EF4444"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Actions -->
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <!-- Acknowledge Button -->
                                    <Button Command="{Binding DataContext.AcknowledgeAlertCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}"
                                           Background="#10B981" 
                                           Foreground="White" 
                                           BorderThickness="0" 
                                           Padding="8,4"
                                           Margin="2"
                                           Cursor="Hand"
                                           ToolTip="Acknowledge">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Content" Value="?"/>
                                                <Setter Property="IsEnabled" Value="True"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsAcknowledged}" Value="True">
                                                        <Setter Property="IsEnabled" Value="False"/>
                                                        <Setter Property="Background" Value="#D1D5DB"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- View Details Button -->
                                    <Button Content="??" 
                                           Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}"
                                           Background="#3B82F6" 
                                           Foreground="White" 
                                           BorderThickness="0" 
                                           Padding="8,4"
                                           Margin="2"
                                           Cursor="Hand"
                                           ToolTip="View Details"/>

                                    <!-- Delete Button -->
                                    <Button Content="??" 
                                           Command="{Binding DataContext.DeleteAlertCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}"
                                           Background="#EF4444" 
                                           Foreground="White" 
                                           BorderThickness="0" 
                                           Padding="8,4"
                                           Margin="2"
                                           Cursor="Hand"
                                           ToolTip="Delete"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
